experiment:
  seed: 42

env:
  name: MiniGrid-FourRooms-v0
  max_steps: 500

policy:
  dropout_rate: 0.2
  net_arch:
    pi: [64]
    vf: [64]

training:
  total_timesteps: 2_000
  n_steps: 256
  batch_size: 64
  learning_rate: 3.0e-4
  eval_freq: 500
  n_eval_episodes: 3

device: cpu
